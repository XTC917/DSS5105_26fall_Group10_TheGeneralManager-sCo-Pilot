PGHOST=localhost
PGPORT=5432
PGDATABASE=factory_copilot_db

PGSCHEMA=app
PG_ADMIN_META_SCHEMA=admin_meta
PG_COPILOT_SCHEMA=copilot
PG_AUTH_SCHEMA=auth

PGUSER=factory_agent
PGPASSWORD=<你本机 factory_agent 的数据库密码>（可以统一为agent）

PG_ADMIN_USER=factory_admin
PG_ADMIN_PASSWORD=<你本机 factory_admin 的数据库密码>（可以统一为admin）

PGCONNECT_TIMEOUT=5
PG_STATEMENT_TIMEOUT_MS=30000
PG_READ_STATEMENT_TIMEOUT_MS=5000

ADMIN_REGISTRATION_CODE=<自己设置一个管理员注册安全码> （可以统一为ARC2026）
AUTH_SECRET_KEY=<自己生成的随机密钥，至少32位>

随机生成 AUTH_SECRET_KEY：python -c "import secrets; print(secrets.token_urlsafe(48))"